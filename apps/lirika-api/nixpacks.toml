[phases.setup]
nixPkgs = ['nodejs']

[phases.install]
cmds = ['npm install --legacy-peer-deps']

[phases.build]
cmds = ['nx run lirika-api:build:production']

[start]
cmd = 'nx run lirika-api:serve:production'
